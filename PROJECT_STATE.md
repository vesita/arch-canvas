# arch-canvas 项目状态

> 一句话：人与 AI **共用同一张 Mermaid 架构图**的 DSH 插件。源文本是唯一真相 ——
> 画布、提示词、代码锚点都是它的视图。
>
> 纪律在 `AGENTS.md`，用户向的说明在 `README.md`，这里只写**状态**：
> 现在跑成什么样、哪些已知没闭、**明确决定不做**什么、下一段怎么推进。

## 现在长什么样

- **落点**：主窗口子页「架构画布」（`conversation.view`，与「对话」「轨迹」并列）。
- **数据**：项目图库 `.arch-canvas/` 里的 `.mmd`（源文本）+ `notes.json`（留言旁路表）；
  没有图库时读路径什么都不建（opt-in）。
- **对外**：4 个 AI 工具（`arch_read` / `arch_edit` / `arch_write` / `arch_switch`）、每步注入的当前状态、
  按需加载的 skill。
- **界面**：拖拽 / 连线 / 检查器 / 源码页 / 导出 / 留言 / 代码锚点 / 检查点。

## 怎么验

```sh
npm run check
```

这道门是 typecheck + 全部测试 + 工具 schema 三段，任何一段红都算没过。
当前基线：**解析器 1056 · 布局 19 · 连线几何 44 · 宿主 903 · 引导层 20 · 插件挂载 16 · 配置卡 37 · 构建守门人 35 · 界面渲染 556 · 工具 schema 全通过 = 2686 条断言，EXIT=0**。
基线数字由 `test/docs.baseline.mjs` 采集各测试自报值并核对，加了断言就要一起改这里和 `README.md`。
装机是**快照**（`file:...tgz` → `~/.dsh/profiles/web/node_modules/arch-canvas/`）：客户端改动 = `npm run build` + 拷 `lib/*.js` + 刷新页面；宿主改动还要**重启 dsh**。生效路径见 `AGENTS.md`。

## 已知未闭（不影响正常使用，撞到再说）

- **空白会话没有入口**：画布那个槽只在有会话时渲染。代价：新会话里打不开画布。
- **`@` 候选空窗**：某个会话从没打开过画布时没有候选。代价：第一次打 `@` 列不出节点；打开一次即有。
- **选中与详情不进跨页记忆**：切走画布等于卸载整个面板。代价：切回来要重新选、详情草稿归零；
  还回来反而会让上一次的正文卡在「回车即生效」的框里。
- **连线是正交折线，不是路由器**：密集时仍可能贴着别的方块走。代价：图密了要手动让开。

## 明确不做（分析出来的结论，不许自己复活）

- **字段表生成** parse/serialize/normalize/snapshot：写盘前的往返守恒检查已经拦住「字段写不出去」，
  收益不够（见 `AGENTS.md` 的「写盘前的往返守恒检查」）。
- **`arch_import` / `arch_check`**：搁置期内不做，依据是下面那份调查；触发条件是「撞到第二次」。
- **LLM 直接生成框架图**：只在「有正式声明 / 目录粒度 / 图当约束」时才可靠。
- **借别人的语法 / 布局**：`lhead/ltail`、`%% @rank same`、Structurizr 式多视图、libavoid / elkjs WASM。
- **从仓库里删旧数据**：`~/.dsh/arch-canvas/architecture.mmd`（全局兜底）与三天前的日志都留着 ——
  删不可逆，留着不花代价。

## 下一段：按体验走

摩擦当场记（在对应节点上留言，AI 读到一次就消耗掉）。只处理「手上撞到的」+「它导致的静默错数据」；
AI 提出或分析推出的需求先记下，不动手。满足任一条就回炉：同一处摩擦出现**第二次** / 出现
**不可逆**的（改了没生效、东西没了、图走样了）/「明确不做」里某项被真实需求撞上两次。

## 调查记录：能不能自动构建初始框架图

结论，省得下次从头论证：

- **抽取便宜，但漏了不报错**：正则补两轮后在 1888 条 Python 引用上追到 1323 / 1326，漏的是枚举不完的写法。
- **决议贵**：真正花钱的是把引用解析到具体文件，要按生态写规则；Rust 里 175 / 423 是 `super::` / `self::`。
- **语义是人类的活**：在这份图自己身上，import 工具得到 0 条边（连接介质是构建脚本的拼接表），
  人画的边才是「渲染 / 上下文注入 / 锚点」。
- **业界没有纯文本正则的工具**：都带 AST，并把「模块名 → 文件」交给生态解析器。
- **活下来的都不是生成器，是一致性检查器**：人先声明架构，机器只负责比对与报警。

两处外部依据：[依赖抽取的召回率](https://2020.icse-conferences.org/details/icse-2020-papers/47/On-the-Recall-of-Static-Call-Graph-Construction-in-Practice)、
[Reflexion Model](https://www.computer.org/csdl/journal/ts/2001/04/e0364/13rRUIIVleg)。
