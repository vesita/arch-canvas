# arch-canvas 项目状态

> 一句话：人与 AI **共用同一张 Mermaid 架构图**的 DSH 插件。源文本是唯一真相 ——
> 画布、提示词、代码锚点都是它的视图。
>
> 纪律在 `AGENTS.md`，用户向的说明在 `README.md`，这里是**状态**：
> 现在能跑成什么样、哪些已知没闭、**明确决定不做**什么、下一段怎么推进。

## 现在长什么样

- **落点**：主窗口子页「架构画布」（`conversation.view`，与「对话」「轨迹」并列，order 30）。
  之前挂在右侧栏 + 左下角入口，2026-09-21 搬过来。
- **数据**：项目图库 `.arch-canvas/` 里的 `.mmd`（源文本）+ `notes.json`（留言旁路表）。
  没有图库时读路径什么都不建（opt-in）。
- **对外**：4 个 AI 工具（`arch_read` / `arch_edit` / `arch_write` / `arch_switch`）+ 每步注入的状态 +
  按用的 skill；界面上是拖拽/连线/检查器/源码页/导出/留言/锚点/检查点。

## 怎么验（一条命令）

```sh
npm run check
```

当前：`mermaid 162` · `layout 19` · `edges 35` · `host.e2e 542` · `host-loader 20` · `plugin-mount 16` ·
`client-config-card 23` · `ui.render 417` · `tools.schema`（全过）= **1234 条断言，EXIT=0**。

装机是**快照**（`file:...tgz` → `~/.dsh/profiles/web/node_modules/arch-canvas/`）：
客户端改动 = `npm run build` + 拷 `lib/*.js` + 刷新页面；宿主改动还要**重启 dsh**。

## 2026-09-23（二）：画布串项目 —— 会话隔离

**事故**：别的会话（别的项目）把共享画布切到它自己那边之后，本会话每一步的提示词注入都读到那张图，
并把留在那上面的 3 条留言当自己的「读一次即送达」消费掉了（用户写的东西投给了错的会话；那 3 条由
对方转写到协作板，才没真丢）。根因：**宿主只有一份内存文档，而 `promptText` 是同步求值、拿不到会话**。

修法（只动宿主半边）：

| 改了什么 | 在哪 | 为什么 |
|---|---|---|
| `promptText(asctx)` 按会话判归属，不属于就只注入一段说明 | `plugin.ts` | DSH 会把 `{agent,…}` 传给注册的 `text` 函数，`agent.session.cwd` 就是这一步的项目 |
| 每个项目一份内存画布（`docSlots`，指针切换） | `document.ts` | 换回来不读盘、不 bump 修订号，投递过的留言状态也留着 |
| 槽只存「已载入」状态；换项目给全新 doc 对象 | `document.ts` | 中间态会被当成命中（空图）；原地改会把别人那份一起改掉 |
| 归属键取项目根（`projectKeyToTarget`） | `document.ts` | 否则一下钻子图库，自己的提示词就被判成别人的 |
| 读路径不发起加载 | `plugin.ts` | 与别会话切库抢活动指针，实测把空文档写进某个项目的槽 |
| `doc.workspace` 进 `RUNTIME_ONLY_FIELDS` | `mermaid.ts` | 不进文件，漏了就是每次保存都误报往返检查（当场被逮住） |

验收：`npm run check` **1234 条断言 EXIT=0**（宿主 542，含新增【会话隔离】22 条）。
残留（未修）：取不到 cwd 的会话仍按「当前文档」读写（既有行为）。

## 2026-09-23：画布交互三件（拖动 / 详情面板 / 连线锚定）

用户报的三处手感问题，按「先调研成熟方案、再借鉴」的路径做。**先说证据，再说做了什么**：

| 问题 | 实测（改之前） | 改之后 |
|---|---|---|
| 连线锚点落在包围盒上 | 菱形一侧 2 条线时锚点离轮廓 **24.4px**、椭圆 10.1px、六边形 5.4px | 8 种形状 × 两种摆放 × 1~4 端口，**最差 0.06px**（有符号距离） |
| 选边只看中心差 | 420 个相对位置平均比同轴最优多绕 **24.8px**、**26.7%** 的位置多绕 40px 以上 | **3.4px / 0%**；回头线与「从背后出线」都是 0 次 |
| 硬穿兜底（绕行只试两条车道） | `dsh-plugin-framework`（28 节点 35 连线）上 5 条线穿过第三方块 | **0 条**（选边变聪明之后一度到 7 条，把兜底车道铺开后归零；总折线还短了 9%） |
| 吸附阈值是模型单位 | 缩放 0.5 倍时屏幕阈值只剩 4px、2 倍时 16px；无迟滞、分组拖动完全不吸 | 阈值 = 8/k（屏幕像素）、8→14px 迟滞、Alt 关闭、分组同一条路 |
| 每帧 `Math.round` + 每帧整树重渲染 | 拖动期坐标按 k 像素跳；`pointermove` 与帧不对齐（jsdom 量到 23 节点 5.7ms/次、80 节点 18.3ms/次） | rAF 合帧（一帧一次状态更新）+ 浮点跟手、提交时才取整 |
| 详情面板只有布尔开关 | 收面板的唯一途径是清选中；点空白/平移都会连带清掉选中；Esc 第二下就把选中丢了 | 三态（展开 / 细条 / 无选中）+ 点同一元素再点收起 + 标题栏「收起 / ×」+ Esc 分层 + 分隔条拖高度 |
| 两个语义 bug | `pointercancel` 被当成正常抬手（会把没完成的拖动写盘进历史）；`lostpointercapture` 静默丢掉已拖出的位移 | 取消 = 回滚；捕获丢失 = 提交 |

**明确不做的**（都是调研后判断代价与收益不成比例，不是没看见）：命令式 DOM 拖动（`.ac-node`
是绝对坐标、要同步边/组框/角标，且 SVG 没有合成层）、把检查器搬到右侧栏（会动整个面板布局，
这次的痛点是「收起/关闭不分」而不是位置）、16 组合枚举 + 混轴 L 形布线器、mxGraph 的
routePatterns 查表、ELK（整图重排会毁掉用户摆的坐标）。

**独立复核（另一个只读子代理）**抓到 1 高危 + 2 中 + 4 低，全部在当轮修掉：
①非矩形锚点让自穿透检查失效 → 整条线硬穿别的方块（8 种形状全中，见上面「三条实现注意」3）；
②两个方块坐标完全重合时连线退化成一个点（`M x y`，看不见也点不中）→ 改走横轴；
③分隔条 window 监听没摘（触屏被 cancel 后会「不按键也改高度」）→ 补 pointercancel 与卸载清理；
④删节点后 dockForRef 没复位（撤销回来第一下点成「收起」）→ 删的时候一起复位；
⑤[4p] 平行间隔断言**空转**（复核复算证明删掉间隔算法照样过）→ 换成穷举搜出来的坐标 + 「两条都是干净 Z」；
⑥[4w] 的负向对照是恒真式 `6 < 8`、还把滚轮步长抄进了测试 → 改成从画布 transform 读缩放；
⑦几何测试只量锚点不量整条路 → 补 128 例「不穿别人也不穿自己」。

**落点**：只改客户端半边（`src/client/runtime.ts` + `studio.ts`），**没有新增任何跨到宿主的字段** ——
所以「半新客户端 + 旧宿主」这条危险路径不存在：`npm run build` → `npm run pack` →
把 `lib/ui.js` 拷进快照安装目录 → **刷新页面**即可（不必重启 dsh）。
装机确认：`curl -s http://127.0.0.1:3080/arch-canvas/ui.js | grep -c ac-sash` 非 0。
真机冒烟：`.arch-canvas/architecture.mmd`（24 节点 27 连线）0 条 null、0 条穿过第三方块。
纪律与守门人写进 `AGENTS.md`「画布交互三件」一节（含两个踩过的坑：胶囊圆角的 `(spanD - r)` 偏移、
六边形左右两侧的公式），新增 `test/edges.test.cjs` 27 条。

## 2026-09-21 又一轮：`arch_drift`（锚点保鲜）

按调查记录的结论，先做零决议知识的那一档。**做完的东西**：

- 锚点**内容指纹**（`anchors.json` 旁路表，只在落盘时记）→ `drift.stale = 图上一次写进文件之后
  这些锚点的文件改过了`；`uncovered = 有源码却没被任何锚点指向的目录`（只报最浅一层）。
- 界面：`▤` 角标三态（蓝 ok / 琥珀 改过 / 红 失效）+ 画布页横幅（有 stale 才用警告色，
  只有 uncovered 走中性 `.hint`）+ 检查器里逐条写原因。
- 提示词：只有非空时才注入一小节「图的保鲜状态」，两档语气（**不许一律喊「过期」**）。
- 守门人：host.e2e【锚点保鲜 drift】34 条 + ui.render 第 [4t] 节 11 条。
  负向对照实测：把 `'drift'` 从 `RUNTIME_ONLY_FIELDS` 删掉 → 往返检查当场红两条；
  拆掉「没有基线就不猜」→ 对应断言红；绕过旁路表闸门 → 坏表被覆盖且丢失拒绝日志。
- **一处真实的覆盖漏洞是负向对照照出来的**：第一版的测试在「加载之后」才加锚点，
  于是「有锚点、没基线」这个场景根本没走到，把守卫拆掉照样绿。改成锚点一开始就在文件里。

## 最近一轮（2026-09-21）：三条静默错数据 / 体感问题

| | 问题 | 修法 |
|---|---|---|
| C | `@` 引用源只有一份模块级快照，而画布切走就卸载 —— 在另一个会话（另一个项目）里打 @，列出来的是**上一次打开的那张图**的节点，展开进 prompt 的也是旧描述 | 快照按会话分开（`studioSnapshots`）；拿不到就返回空。`codec.serialize` 拿不到会话（契约如此），所以解析走 `resolveLiveNode`：活跃会话优先，两个会话同名而活跃的没有 → **拒绝展开** |
| A | 画布是主窗口子页、**一次只渲染一个**：切走 = 卸载，缩放/平移与 60 步撤销历史全归零 | 模块级 `studioMemo`（`<会话>\|<文档>`），**只记用户自己动过的**（滚轮/平移/适应窗口/清单定位；撤销栈非空）；不记内容、不记选中、不还 `committedRef` |
| B | 宿主的 `lastChange` 随 `doc:get` 一直回来，切回画布就把**同一次**旧 AI 改动再脉动 5.2 秒、顶栏再喊一遍 | 客户端按「文档 + 修订号」去重（`memo.hlKey`） |
| — | `%% @file` 的值把 `#` 写成 `#35;`，源码页里人看到的就是它 | 注释里的元数据值不再套标签那套实体转义（`qRef`）；读回仍认 `#35;`，老文件下一次落盘规整 |

证据：`test/mermaid.test.cjs` 第 [20]、`test/ui.render.mjs` 第 [4s]（**9 条**，含四条负向对照：
问别的会话 / 换图不继承 / 旧高亮不重放 / 含糊时拒绝展开。四个负向对照都实测「撤掉修复就红」）。

## 2026-09-21 这一轮：观感 / 布局 / 留言语义

三块都不小，但共同点是**都把「靠感觉」换成了「靠数字或靠结构」**：

1. **观感**：节点描边 / 连线 / 组框用的都是 `--dsw-alias-border-*`（12% 与 6% 的白），
   实测对比度 1.48 / 1.42 / 1.17 : 1，而 WCAG 对图形要求 ≥3:1 —— 用户说的「淡」是三个数。
   换成自带的 `--ac-line`，并给组加了 8 档**亮度归一**的色相（同一个 L 下蓝比绿暗三倍，
   所以按相对亮度而不是 HSL 的 L 归一）。导出件（白底字面色板）同步，否则屏幕花的、导出还是灰的。
2. **自动布局**：不是「差一点」，是坏的 —— 分层靠 `cand < nodes.length` 当刹车，图里一有环就把
   节点一层层推到上界。本仓库那张 28 节点的图变成层号 0..27（20 层是空的）、35 条边压出 83 个交叉。
   改成先折强连通分量再分层 + 同层中位数排序（**算完比交叉数，不更少就不换**），83 → 35；
   顺手修掉 `rev` 把累加器翻来翻去导致 BT/RL 三层叠成两层的 bug。
3. **留言从「待办」变成「消息」**：用户原话「传输给一方后就移除，最多留一个历史查询」。
   `promptText()` 读到就算送达，随即在内存里标成已办 —— **僵尸驻守从结构上消失**，
   不再需要任何规矩去防（之前试过「让用户点」「让 AI 自己标」两版，都是在维护一个本可以不存在的状态）。
   历史落盘时按进入时刻丢最旧的，只留 6 条；`at` 不在模型上，所以**没碰**白名单 / 快照那三处。
   代价写在代码注释里：送达记录先进内存，下一次落盘才写进去 —— 之前重启会让它再送一次，
   宁可多送一次也不丢用户写的东西。

**还没做的**：AI 也能写留言（反向通道）、界面措辞的收尾、以及一次性消耗的**体感确认**
（用户还没用过）。另外「长期挂着的意图」不再适合放留言了 —— 它会被投递一次就消失。

## 已知未闭（不影响正常使用，撞到再说）

- **空白会话没有入口**：那个槽只在有会话时渲染，左下角入口已摘掉。
- **`@` 候选的空窗**：某个会话从没打开过画布时没有候选（要拿 cwd 才能现取，
  而 cwd 的客户端来源 `useSessions` 是组件里的 hook）。打开一次即有。
- **选中与详情面板不进跨页记忆**：详情里那堆输入框是「正在改的东西」，还回来容易变成回车即生效的错误正文。
- `test/host.e2e.mjs` 的桩 `fs` 会把测试日志写进生产目录 `~/.dsh/arch-canvas/logs/`，制造 `head` 假警报。
- 连线是正交折线但**不是路由器**：密集时仍可能贴着别的方块走。

## 明确不做（分析出来的，不是手上的痛 —— 不许自己复活）

- **字段表生成** parse/serialize/normalize/snapshot：写盘前的往返守恒检查已经拦住「字段写不出去」，
  收益不够（真值表见 `AGENTS.md` 的「写盘前的往返守恒检查」）。
- **`arch_drift`（锚点腐烂报告）/ `arch_import` / `arch_check`**：**搁置期内都不做**。
  真要复工时的顺序是 `arch_drift` → `arch_import`（见下面那份调查记录），触发条件是「撞到第二次」。
- **LLM 直接生成框架图**：只在「有正式声明/目录粒度/图当约束」时才可靠（CodeSee 已停、Swarm 类
  采样幻觉多）。理由与实测见下面那份调查记录。
- **借别人的语法/布局**：`lhead/ltail`、`%% @rank same`、Structurizr 式多视图、libavoid/elkjs WASM。
- **从仓库里删旧数据**：`~/.dsh/arch-canvas/architecture.mmd`（0.3 时代的全局兜底）、三天前的日志。

## 调查记录：能不能自动构建初始框架图（2026-09-21）

问题：用算法（尤其**纯文本**，不引解析器）自动生成初始框架图，可行到什么程度？
结论留在这里，省得下次从头论证。

**证据来源**：本地 8 个真实仓库上的 9 组测量（Python: `meno` `krice` `nanoSeek` `stross`；
TS/JS: `dsh-antigravity` `dsh-collab`；Rust: `stross` `breeze` `exreg`）。
当时的探针是临时脚本，还留在 `/tmp/ac-auto/`（`probe.py` / `probe-js.mjs` / `probe-rs.py`，
**重启就没了**；TS 那支另需一个临时装的 `@babel/parser`）——
方法是「同一份语料上跑两遍：一遍正则、一遍真解析器，逐条比集合」，要复跑就按这句话重写。
别指望仓库里有现成的。下表的数就是那时候量出来的。

| 环节 | 实测 | 说明 |
|---|---|---|
| 抽取：正则 vs 真解析器（Python 1888 条引用） | 补两轮正则后 1323 / 1326 | 漏的是 `import a, b as c` 这类**枚举不完的写法**；而且**漏了不报错** |
| 抽取：TS/JS 两套策略 | 紧：漏 4–5、误报 0；松：漏 0、**误报 5–6** | 误报来自真实仓库里「代码作为字符串」（`collab-plugin.host.js` 嵌着插件源码）与英文散文 `from 'saved row'` |
| 决议（真正贵的部分） | `meno` 194 条内部引用，**朴素规则 0 条解开**，全靠「`src` 布局」这条工程知识；TS 加一条 `./x.js → x.ts` 让内部边 **59 → 105**、解不开 **46 → 0** | 抽取几乎免费，决议要按生态写规则 |
| Rust | `crate::` 起头 178/230 靠文件名约定解开；但 **175/423 是 `super::`/`self::`**（要先按 `mod` 重建模块树） | `#[path]` / `include!` 在这三个仓库都是 0，约定没被绕开 |
| 单元卫生 | `dsh-antigravity` 46 个文件里 **29 个是构建产物**（63%），15 个 basename 重复出现 | 不先排除就是 2–3 倍假节点与假枢纽 |
| 语义（最关键） | 本图 23 个节点落在 **7 个文件**（一个文件 2.9 个概念）；而 `src/` 13 个 `.ts` 里**只有 5 行 import** | 它的连接介质是 `tools/build.mjs` 的 `HOST_PARTS`/`UI_PARTS` —— **import 工具在 AC 自己身上得到 0 条边**，而人画的 23 条边是「渲染 / 上下文注入 / 锚点 / 留话」 |

外部调研（引用已核对存在）：依赖抽取 P/R 可 >90%，但动态特性下真实调用边会漏掉 20–75%
（[ICSE 2020](https://2020.icse-conferences.org/details/icse-2020-papers/47/On-the-Recall-of-Static-Call-Graph-Construction-in-Practice)）；
**即使喂 100% 准确的依赖图**，自动恢复的逻辑组件也只有 ~55–75% 吻合，而「简单目录基线」经常打败无监督聚类
（[Lutellier ICSE 2015](https://dl.acm.org/doi/abs/10.5555/2819009.2819022)、
[Candela TSE 2018](https://www.computer.org/csdl/journal/ts/2018/02/07859416/13rRUxcsYNK)）；
工业界一致的做法是 [Reflexion Model](https://www.computer.org/csdl/journal/ts/2001/04/e0364/13rRUIIVleg)
—— **人给意图骨架 + 机器提取事实 + 显式映射**，Structure101 / Lattix / Structurizr 都要架构师写切片与规则。
**AC 现在的形状（人画图 + `%% @file` 锚点 + `arch_read` 回读）正是这个形态。**

工具侧（第三份调研，引用抽查过存在）：**没有一个是纯文本正则的** —— 连最轻的
`madge`、`code2flow` 都带 AST（后者的自述是 "pretty good call graphs"，不是 "perfect"）；
CodeCharta 的 `RawTextParser` 只敢数行数与缩进；它们把「模块名 → 文件」这一段交给
`enhanced-resolve` / `tsconfig paths` / `exports` 映射 —— 与我量到的「决议才是最贵的」是同一件事；
barrel 造成的假依赖边有专门的 issue（[dependency-cruiser #938](https://github.com/sverweij/dependency-cruiser/issues/938)）。

而**活下来的都不是「生成器」，是「一致性检查器」**：dependency-cruiser（分层/禁令规则）、
ArchUnit（架构契约）、NDepend（CQLinq）、Structure101（Architecture Specs）——
人先声明架构，机器只负责比对与报警。这条把 `arch_drift` 从「便宜的第一步」升级成「业界验证过的方向」。

（该报告里还引了一条「用 LLM 从代码推断 C4」的 arXiv 论文，我**没能打开验证**
（arxiv 域名在本机不可达），按未核实处理；它也不影响上面的结论。）

因此的取舍：

- ~~先做零决议知识的那一档：`arch_drift`~~ → **已完成**（见上面「2026-09-21 又一轮」）。
- `arch_import` 若做：目录/包粒度、**只出候选不落盘**、每条边带**出处行号**、自带排除名单（测试/产物/生成物）
  与每生态几行决议规则；**「解不开的引用」要当成一等输出列给人看**，不许静默丢。
- **不做**：LLM 直接生成、无监督聚类出「逻辑端」、拿机器骨架覆盖用户摆的坐标。
- **硬约束**：宿主逻辑两种形态共用 `src/host/*`，而动态 Package 形态**没有 `require`/`process`** ——
  所以不能调 `dependency-cruiser` / `pydeps` / `cargo-modules`，只能自己用 `fs` 读文件、自己实现决议。

## 下一段：按体验走（2026-09-21 起）

1. **摩擦当场记**：就在那张图对应的节点上写留言（AI 会读到一次，然后它就消耗掉了），或者直接说一句。
   形式不用讲究，但**别攒着回忆**。
2. **只处理「手上撞到的」+「它导致的静默错数据」**；上面「明确不做」里的东西不许复活。
   由 AI 提出、由分析推出的需求，先记到这里，不动手。
3. **回炉判据**（满足任一条就转回工程视角）：
   - 同一处摩擦出现**第二次**（第一次可能是没习惯）；
   - 出现**不可逆**的（「改了没生效 / 东西没了 / 图走样了」）—— 这类优先于一切体验项；
   - 「明确不做」里某一项被真实需求撞上两次（说明判断错了，改这里的结论并注明原因）。

> 这一段的由来：前几轮靠「审计 + 分析」推进，确实抓到 11 条静默丢数据的 bug，
> 但那一类现在有不变式守着；而剩下的候选（字段表、drift、借语法）都是**推测**出来的痛。
> 这个项目的每个真 bug 都是**用出来的**（滚轮被掐、文本变白、详情弹出、端口悬空）。
